source "https://rubygems.org"

gem "jekyll", "~> 3.8"
# with Ruby 2.x on OS X, you may need to install
# it manually with --with-iconv-dir:
#
# gem install iconv -- --with-iconv-dir=~/.rvm/usr
gem 'iconv'
gem 'albino'
gem 'redcarpet'
gem 'rouge'
gem 'rake', '~> 12.3'
